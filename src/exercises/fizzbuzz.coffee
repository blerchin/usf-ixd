module.exports = (num) ->
# Write a function that:
# - returns "Fizz" if num is divisible by 3
# - returns "Buzz" if num is divisible by 5
# - returns "FizzBuzz" if num is divisible by 3 and 5
# - returns num if none of these conditions apply
