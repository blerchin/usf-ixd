sudo: required

services:
  - docker

before_install:
  - pip install --user awscli
  - sudo apt-get install -y jq

script:
  - docker build . -f test/Dockerfile -t usf-ixd --build-arg GITHUB_USERNAME --build-arg GITHUB_ACCESS_TOKEN
  - docker run usf-ixd
